version: '3.7'
services:
  redis:
    image: redis:alpine
    volumes:
      - redis-data:/data
  logstream:
    build: .
    environment:
      REDIS_URL: redis://redis:6379
    restart: always
volumes:
  redis-data:
