apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openviking
  annotations:
    tailscale.com/hostname: "openviking"
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: openviking
      port:
        number: 1933
  tls:
    - hosts:
        - openviking
