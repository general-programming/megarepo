apiVersion: v1
kind: Namespace
metadata:
  name: freeipa
  labels:
    kubernetes.io/metadata.name: freeipa
    # virt-launcher needs it.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
