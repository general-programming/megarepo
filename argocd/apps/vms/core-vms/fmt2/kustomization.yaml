# Intentionally empty: these are sea1 guests. The overlay still has to exist,
# or the fmt2 Application resolves to a missing path and sits Unknown forever.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
