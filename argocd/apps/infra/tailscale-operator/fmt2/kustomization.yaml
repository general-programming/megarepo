---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: tailscale-operator
resources:
  - ../base
helmCharts:
  - name: tailscale-operator
    releaseName: tailscale-operator
    repo: https://pkgs.tailscale.com/helmcharts
    version: 1.96.5
    namespace: tailscale-operator
    valuesInline:
      includeCRDs: true
      operatorConfig:
        hostname: "tailscale-operator-fmt2"
