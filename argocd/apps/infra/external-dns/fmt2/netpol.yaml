---
# Egress only, for the same reason as reloader: inbound is probes and scraping,
# which a short sample cannot enumerate safely.
#
# Observed egress, whole sample: coredns:53, kube-apiserver:6443, and world:443
# for the DNS provider API. The egressDeny is the part that earns this policy --
# world:443 would otherwise include the house, so RFC1918, the MetalLB VIPs,
# link-local metadata and bogons are denied explicitly.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: external-dns-egress
  namespace: external-dns
  labels:
    fmt2.policy/tier: infra
spec:
  endpointSelector:
    matchLabels:
      app: external-dns
  egress:
    - toEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: ANY
    # Pinned to 6443: Talos serves the apiserver on the nodes, so the
    # kube-apiserver label rides the node identity and an unported allow would
    # open every port on the control-plane IPs (kubelet, etcd, apid).
    - toEntities:
        - kube-apiserver
        - host
        - remote-node
      toPorts:
        - ports:
            - port: "6443"
              protocol: TCP
    - toEntities:
        - world
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
  egressDeny:
    - toCIDRSet:
        - cidrGroupRef: rfc1918
        - cidrGroupRef: fmt2-loadbalancers
        - cidrGroupRef: link-local-metadata
        - cidrGroupRef: bogons
