---
# Cilium agent: :9962 general agent/datapath/policy metrics, :9965 Hubble
# flow-derived metrics. Both live on the same DaemonSet pod.
#
# VM-native VMPodScrape rather than the chart's prometheus.serviceMonitor,
# which emits a prometheus-operator ServiceMonitor -- this cluster runs the
# VictoriaMetrics operator. Same reasoning as agent_scrape_cnpg.yaml.
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMPodScrape
metadata:
  name: cilium-agent
  namespace: victoriametrics
spec:
  podMetricsEndpoints:
    - port: prometheus
      path: /metrics
    - port: hubble-metrics
      path: /metrics
  namespaceSelector:
    matchNames:
      - kube-system
  selector:
    matchLabels:
      k8s-app: cilium
---
# Operator metrics: identity allocation, ipam, garbage collection, k8s client.
apiVersion: operator.victoriametrics.com/v1beta1
kind: VMPodScrape
metadata:
  name: cilium-operator
  namespace: victoriametrics
spec:
  podMetricsEndpoints:
    - port: prometheus
      path: /metrics
  namespaceSelector:
    matchNames:
      - kube-system
  selector:
    matchLabels:
      io.cilium/app: operator
