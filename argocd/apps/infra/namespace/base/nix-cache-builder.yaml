apiVersion: v1
kind: Namespace
metadata:
  name: nix-cache-builder
  labels:
    kubernetes.io/metadata.name: nix-cache-builder
    # Nix's build sandbox needs mount + user namespaces, so the builder pod runs
    # privileged. Restricting it would just force `sandbox = false` instead.
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
