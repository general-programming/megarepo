apiVersion: v1
kind: Namespace
metadata:
  name: akvorado
  labels:
    # clickhouse-operator pods add IPC_LOCK/PERFMON/SYS_PTRACE, which the
    # cluster-default baseline PodSecurity rejects; same as the sentry ns.
    pod-security.kubernetes.io/enforce: privileged
