# Home for KubeVirt guests themselves, kept apart from the `kubevirt`
# namespace where the operator and its components live.
apiVersion: v1
kind: Namespace
metadata:
  name: virt
  labels:
    kubernetes.io/metadata.name: virt
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/enforce-version: latest
