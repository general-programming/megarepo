version: '2'
services:
  victoriametrics:
    image: victoriametrics/victoria-metrics:latest
    restart: always
    command:
      - "-retentionPeriod=12"
    volumes:
      - /var/lib/victoria-metrics-data:/victoria-metrics-data
    ports:
      - 8428:8428
