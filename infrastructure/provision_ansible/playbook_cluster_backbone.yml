- hosts: all_hosts
  become: true
  roles:
    - role: update_packages
    - role: setup_tincd
      when: tinc_ip is defined and tinc_ip
    - role: update_configs
    # - role: create_admin_user
    - role: setup_influxdb
      when: influx_listen_ip is defined and influx_listen_ip
    - role: setup_telegraf
      when: influx_listen_ip is defined and influx_listen_ip
    # - role: setup_collectd
  vars:
    influx_host: 192.168.3.2
    syslog_server: 192.168.3.2
    syslog_port: '5140'
