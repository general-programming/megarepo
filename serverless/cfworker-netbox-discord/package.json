{
  "private": true,
  "name": "cfworker-netbox-discord",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "author": "erin <erin@generalprogramming.org>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.8.2",
    "wrangler": "^4.81.1"
  }
}
