# Managed by salt (salt_minion state). Multi-master, active-active; the
# minion.d include supersedes the single-master list in /etc/salt/minion.
master:
  - 10.3.2.6    # sea1-core
  - 10.65.67.5  # fmt2-core (vlan5, routed from 10.0.0.0/8)

master_type: str
random_master: true
